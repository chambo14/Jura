/.phpunit.cache
/node_modules

# Base locale : contient les collaborateurs, leurs e-mails et les données projets.
/database/*.sqlite
/database/*.sqlite-journal
/database/*.sqlite-shm
/database/*.sqlite-wal

# Les feuilles de style compilées sont versionnées à dessein : le poste de la
# Direction des Projets n'a pas les droits d'installer npm, et l'hébergement
# n'a ni SSH ni Node. Sans elles dans le dépôt, personne ne pourrait
# reconstruire le style après un changement de vue.
# /public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
